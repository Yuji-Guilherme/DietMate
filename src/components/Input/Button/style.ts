const iconWrapperClassName =
  'w-8 flex items-center justify-center h-full bg-transparent pr-2 border-solid border-slate-300 border-r-1 group-focus-within:border-slate-600 focus:outline-none';

const svgIconClassName = 'w-3 h-3 ml-[2px]';

const pathIconClassName =
  'stroke-slate-400 group-focus-within:stroke-slate-600 transition-colors';

export { iconWrapperClassName, svgIconClassName, pathIconClassName };
